<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AmzoodMart" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bule</item>
        <item name="colorPrimaryDark">@color/pink</item>
        <item name="colorAccent">@color/white</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="splashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/maingreen</item>
        <item name="colorAccent">@color/text</item>
        <item name="android:windowBackground">@drawable/splash_activity_background</item>
        <!-- Customize your theme here. -->
    </style>
</resources>